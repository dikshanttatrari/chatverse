<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatVerse" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/brown</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
    </style>

    <!-- Light theme for both day and night modes -->
    <style name="Theme.ChatVerse" parent="Base.Theme.ChatVerse" />

    <style name="Theme.ChatVerse.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.ChatVerse.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
